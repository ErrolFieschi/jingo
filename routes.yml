
/:
  controller: Main
  action: default
  auth: false

/admin:
  controller: Security
  action: default
  auth: false

/dashboard:
  controller: Security
  action: admin
  auth: false

/stats:
  controller: Main
  action: stats
  auth: false

/settings:
  controller: Main
  action: settings
  auth: false

/register:
  controller: Security
  action: register
  auth: false

/login:
  controller: Security
  action: login
  auth: false

/logout:
  controller: Security
  action: logout
  auth: true

/setup:
  controller: Security
  action: setup
  auth: false

/home:
  controller: Main
  action: home
  auth: false
  role: user

/training:
  controller: Training
  action: training
  auth: false

/training/options:
  controller: Training
  action: trainingOptions
  auth: false

/training/delete:
  controller: Training
  action: trainingDelete
  auth: false

/training/visible:
  controller: Training
  action: trainingVisible
  auth: false

/model:
  controller: Front
  action: model
  auth: false

/users:
  controller: Main
  action: users
  auth: false

/forget-password:
  controller: Security
  action: forgetPassword
  auth: false

/admin-user:
  controller: User
  action: user
  auth: false

/admin-user/update:
  controller: User
  action: updateUser
  auth: false

/lesson/update:
  controller: Part
  action: updateLesson
  auth: false

/lesson/delete:
  controller: Lessons
  action: deleteLesson
  auth: false

/part/delete:
  controller: Part
  action: deletePart
  auth: false

/part/sort:
  controller: Part
  action: sortPart
  auth: false

/pages:
  controller: Page
  action: showPages
  auth: false

/page-creator:
  controller: Page
  action: createPage
  auth: false

/page/render:
  controller: Page
  action: renderPage
  auth: false

/page/update:
  controller: Page
  action: updatePage
  auth: false

/navbar:
  controller: Navbar
  action: navEdit
  auth: false

/library:
  controller: Library
  action: library
  auth: false

/courses:
  controller: Training
  action: list
  auth: false

/lesson/display:
  controller: Lessons
  action: display
  auth: false
