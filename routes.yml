
/:
  controller: Main
  action: default
  auth: false

/admin:
  controller: Security
  action: default
  auth: true

/dashboard:
  controller: Security
  action: admin
  auth: true

/stats:
  controller: Main
  action: stats
  auth: true
  role: admin

/settings:
  controller: Main
  action: settings
  auth: false

/register:
  controller: Security
  action: register
  auth: false

/login:
  controller: Security
  action: login
  auth: false

/logout:
  controller: Security
  action: logout
  auth: true

/setup:
  controller: Security
  action: setup
  auth: false

/home:
  controller: Main
  action: home
  auth: false
  role: user

/training:
  controller: Training
  action: training
  auth: true

/training/options:
  controller: Training
  action: trainingOptions
  auth: true

/training/delete:
  controller: Training
  action: trainingDelete
  auth: true

/training/visible:
  controller: Training
  action: trainingVisible
  auth: true

/model:
  controller: Front
  action: model
  auth: false

/forget-password:
  controller: Security
  action: forgetPassword
  auth: false

/admin-user:
  controller: User
  action: user
  auth: false

/admin-user/delete:
  controller: User
  action: deleteUser
  auth: false

/lesson/update:
  controller: Part
  action: updateLesson
  auth: false

/lesson/delete:
  controller: Lessons
  action: deleteLesson
  auth: false

/part/delete:
  controller: Part
  action: deletePart
  auth: false

/part/sort:
  controller: Part
  action: sortPart
  auth: false

/pages:
  controller: Page
  action: showPages
  auth: true

/page-creator:
  controller: Page
  action: createPage
  auth: true

/page/render:
  controller: Page
  action: renderPage
  auth: true

/page/update:
  controller: Page
  action: updatePage
  auth: true

/navbar:
  controller: Navbar
  action: navEdit
  auth: false
