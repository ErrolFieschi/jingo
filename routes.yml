
/:
  controller: Main
  action: default
  auth: false

/admin:
  controller: Security
  action: default
  auth: true

/dashboard:
  controller: Security
  action: admin
  auth: false

/stats:
  controller: Main
  action: stats
  auth: false

/settings:
  controller: Main
  action: settings
  auth: false

/s-inscrire:
  controller: Security
  action: register
  auth: false

/se-connecter:
  controller: Security
  action: login
  auth: false

/login:
  controller: Security
  action: login
  auth: false

/se-deconnecter:
  controller: Security
  action: logout
  auth: true

/home:
  controller: Main
  action: home
  auth: false

/training:
  controller: Main
  action: training
  auth: false

/pages:
  controller: Pages
  action: pages
  auth: false

/model:
  controller: Front
  action: model
  auth: false

# LINKS FOR THE FRONT - PUT THE CONFIG ON THE TOP
/form:
  controller: Front
  action: form
  auth: false

/rdv:
  controller: Front
  action: rdv
  auth: false

/anniversaire:
  controller: Front
  action: anniversaire
  auth: false

/calendrier:
  controller: Front
  action: calendrier
  auth: false


